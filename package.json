{
  "name": "wasmpack-starter",
  "private": true,
  "scripts": {
    "dev": "run-p dev:*",
    "dev:wasm": "cd wasm && pnpm dev",
    "dev:web": "cd web && pnpm dev",
    "build": "turbo run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "turbo": "^1.2.4"
  }
}
